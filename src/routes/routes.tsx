import {
  Switch,
  BrowserRouter,
} from 'react-router-dom';

// import Private from './private';
// import Public from './public';

const Routes = () => (
  <BrowserRouter>
    <Switch>
      {/* <Private path={PATHS.} exact component={} /> */}
      {/* <Public path={PATHS.} exact component={} /> */}
    </Switch>
  </BrowserRouter>
);

export default Routes;
